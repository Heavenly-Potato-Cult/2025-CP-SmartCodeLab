<UserControl x:Class="SmartCodeLab2.CustomComponents.CustomSideMenuButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Height="50" Width="165">
    <Button x:Name="PART_Button" 
            Background="Transparent" 
            BorderThickness="0" 
            Padding="0"
            HorizontalContentAlignment="Stretch"
            VerticalContentAlignment="Stretch" Height="30" Width="127">

        <Grid VerticalAlignment="Center">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="30"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Icon -->
            <Image Source="{Binding Icon, RelativeSource={RelativeSource AncestorType=UserControl}}"
                   Width="16" Height="16"
                   VerticalAlignment="Center" HorizontalAlignment="Center"/>

            <!-- Text -->
            <TextBlock Grid.Column="1" 
                       Text="{Binding Label, RelativeSource={RelativeSource AncestorType=UserControl}}" 
                       Foreground="White" FontSize="12"
                       VerticalAlignment="Center" Height="16" />
        </Grid>
    </Button>
</UserControl>
